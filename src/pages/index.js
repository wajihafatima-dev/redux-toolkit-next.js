import React from 'react';
import Cards from './cards/Cards';

const Home = () => {
    return (
        <div>
            <h1>Simple API Fetch with Redux Toolkit</h1>
            <Cards />
        </div>
    );
};

export default Home;
